{
    "scripts": {
        "start": "npm install && npm run build",
        "dev": "nodemon app.js",
        "build": "./node_modules --out-dir public"
    },
    "dependencies": {
        "express": "^4.18.2"
    }
}
